server:
  port: 8081
spring:
  application:
    name: admin-server
  mail:
    host: smtp.qq.com # 发送邮件服务器
    username: xxxxxx@qq.com # QQ邮箱
    password: opvjwamgyknkebce # 客户端授权码
    protocol: smtp # 发送邮件协议
    properties:
      mail:
        smtp:
          auth: true # 安全认证(默认是true)
          port: 465 # 端口号465或587
          starttls:
            enable: true
            required: true
          ssl:
            enable: true # 开启ssl加密 否则项目启动时报530error
        display:
          sendmail: Javen # 可以任意
          sendname: Spring Boot Guide Email # 可以任意
    default-encoding: utf-8
    from: xxxxxx@qq.com # 与上面的username保持一致
  boot:
    admin:
      notify:
        mail:
          from: yemingrujing@163.com
          to: xxxxxx@qq.com


# tag::configuration-eureka[]
eureka:   #<1>
  instance:
    leaseRenewalIntervalInSeconds: 10
    health-check-url-path: /actuator/health #2.0后actuator的地址发生了变化
  client:
    registryFetchIntervalSeconds: 5
    serviceUrl:
      defaultZone: ${EUREKA_SERVICE_URL:http://root:123456@localhost:8761}/eureka/

# 2.0开始，actuator默认不开放，所以要设置为开放
management:
  endpoints:
    web:
      exposure:
        include: "*"  #<2>
  endpoint:
    health:
      show-details: ALWAYS
# end::configuration-eureka[]

#-----------------------编写者相关信息----------------------
info:
  owner: guangwei
  version: '@project.version@'
  app:
    name: "@project.name@"
    version: "@project.version@"
    description: "@project.description@"